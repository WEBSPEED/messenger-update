# EmaxICT Messenger Update

Public update channel for the EmaxICT Messenger desktop application.

This repository contains only release metadata, installers, and split update
packages. Application source code and groupware server configuration are not
included.

The desktop client verifies file sizes and SHA-256 hashes from
`desktop-update.json` before applying an update.
